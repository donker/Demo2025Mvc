@inherits Demo2WebPage
@using Bring2mind.Demo2025Mvc.Demo2.Common
@{
  //Demo2ModuleContext.AddModuleScript();
}

<h1>Hello DEMO 2 Module with ID @Dnn.ActiveModule.ModuleID</h1>
@if (Dnn.User.UserID > -1)
{
  <div>
    <p>Current User: <span style="font-weight: bold">@Dnn.User.DisplayName</span> with email <span style="font-weight: bold">@Dnn.User.Email</span></p>
  </div>
}

<div>
  <button type="submit" id="btn" class="dnnPrimaryAction">Test postback on home page</button>
</div>

@if (Dnn.User.UserID > -1)
{
  <div>
    <a href="@Url.Action("Index", "Foo", new { UserId = this.Dnn.User.UserID })">Go to your details page</a>
  </div>
}

